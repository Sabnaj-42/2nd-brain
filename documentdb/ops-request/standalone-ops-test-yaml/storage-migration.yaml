apiVersion: ops.kubedb.com/v1alpha1
kind: DocumentDBOpsRequest
metadata:
  name: documentdb-standalone-storage-migration
  namespace: demo
spec:
  type: StorageMigration
  databaseRef:
    name: documentdb
  timeout: 10m
  migration:
    storageClassName: standard-custom
    oldPVReclaimPolicy: Delete
