### Configure.sh

$PGDATA -> /var/pv/data
$SHARED_BUFFERS // use function used in postgres | search for SHARED_BUFFERS
$HOSTNAME
$PRIMARY_HOST
$POSTGRES_USER
$POSTGRES_USER
$SSL_MODE -> off
$CLIENT_AUTH_MODE -> scram
$STANDBY -> hot
$STREAMING -> Asynchronous
$WAL_RETAIN_PARAM -> use from postgrs -> walkeepsize
$WAL_RETAIN_AMOUNT -> 2500

### run.sh
$POSTGRES_PASSWORD

