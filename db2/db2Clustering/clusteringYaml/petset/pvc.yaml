apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db2-data
  namespace: demo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
