apiVersion: ops.kubedb.com/v1alpha1
kind: DocumentDBOpsRequest
metadata:
  name: dcdb-hscale-up
  namespace: demo
spec:
  type: HorizontalScaling
  databaseRef:
    name: dcdb
  horizontalScaling:
    replicas: 5
