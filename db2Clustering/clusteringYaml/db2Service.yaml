apiVersion: v1
kind: Service
metadata:
  name: db2-service
  namespace: db2-system-test
spec:
  selector:
    app: db2
  ports:
    - protocol: TCP
      port: 50000
      targetPort: 50000
  type: ClusterIP

